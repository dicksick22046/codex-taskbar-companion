# Pinned status rows

Replace the independently positioned Running strip with a single lightweight panel attached edge-to-edge to the existing status bar. Each selected nonempty status occupies one 30-DIP row in the existing category order; there are no repeated state headings, counts, times or usage values. A row contains its colored state dot, project tag, task title and an unpin control. The whole visible row, including blank padding, opens that task except for the separate unpin button. Preserve press/release target and drag-cancel protection. Long project names are elided while retaining title space. Full detail and Main/Side identity remain in the existing status list and tooltips. No Agent children are added.

Each status popover has a top-right Pin/Unpin button. Pinning ensures status counts are enabled, persists the chosen category and closes the full list. The bottom status buttons continue opening full lists. Row title activation opens the selected task; unpin only removes that category. Empty pinned categories hide but retain their preference; pinned empty categories remain reachable from the tray status menu to unpin. Remove the settings switch for the independent strip. Migrate an existing enabled strip to Running pinned; preserve explicit new preferences and discard the obsolete independent position.

The panel and bar share a gently shaded surface and fine outer rim, continuous across their join with no internal border. Surface and rim follow the manual transparency setting; foreground colors and hit targets stay unchanged. The one panel uses the same width, color, opacity and connected edges as the bar; size it from the title and footer requirements below. It follows the bar, cannot be dragged separately, opens above where possible and below near the screen top. Hide it when its bar is unavailable or fullscreen is foreground. Every row independently retains the existing 8-second rotation, interruptible handoff and hover-only long-title scroll. Hover visibly highlights the whole row, pauses its rotation, and gives the pin a separate pressed/hover cue. The pinned-row icon is muted at rest and gains accent color on hover, press or keyboard focus. Press/release target protection, reduced motion, hidden-clock suspension, task removal and language changes must remain correct. Accessible names include category and title without adding visible labels.

Use the existing Provider and shared UI cadence, without new task collection, global pointer input or task execution. Verify preference migration, multiple categories, empty/reappearance, pin/unpin without title activation, status changes during press, independent hover timing, attachment above/below, no separate drag, all four languages, reduced motion and real installed visibility. Tests never consume reset credits.

Full detail panels temporarily replace compact rows inside this same native host, as specified in [Attached panel region](interaction.md#attached-panel-region). A divider separates detail from navigation; the host height spring handles expansion and restoration of the saved rows. The context menu stays outside the combined area. The host remains owned by the bar; detail controls are children of the host. Repair broken relative order without activation, changing unrelated window order, or continually forcing topmost; retain the floating topmost preference.

The rotating footer treats each label/value pair as one compact group with a 3-DIP gap. Status pills form a separate group aligned to the right inset, rather than inheriting spare title width as trailing whitespace. Reserve the widest complete metric pair for stable rotation, not the sum of independent widest labels and values. The ring stays fixed. Pinned title width is measured across the current candidates and can shrink the former 240-DIP minimum, capped at that previous allowance; footer requirements still take priority. Do not resize for each rotated task or quota item.

Reuse existing taskbar mouse-release observation and native Z-order-change messages to coalesce one immediate layer/paint reconciliation after shell activation. Preserve foreground focus, avoid extra polling, and suppress re-entry from the repair's own window changes. Periodic checks remain the fallback.

## Running feedback and optional metric names

Running status dots in the footer and attached running row breathe gently in the same phase. A grayscale opacity sweep passes through the running title without shifting its text or controls. Other categories remain still. Reuse the existing 33ms paint clock, repaint only affected visible regions, stop when hidden or no running item remains, and honor reduced motion. Preserve existing ring interpolation, hover reading and task handoff.

The running-title reference is the installed Codex 26.908.4834.0 Thinking/CadencedShimmer component and the user's dark-band screenshot: fixed glyphs with a translated opacity mask, rather than a white highlight. The mask spans half the visible text width with a central plateau; a 1.8-second, 48-step sweep begins after 600ms and repeats every two seconds, leaving only a 200ms pause as requested for nearly continuous activity. The native adaptation keeps the regular title color and fades the moving band toward the actual background, so dark/light/transparent capsules need no extra light color. Cache glyphs by text, font, pixel phase and scale; apply the mask before task-handoff opacity to preserve stable ink and fade strength. Fitting-title hover retains activity, while overflowing titles use steady marquee reading. Hidden/reduced-motion/pressed states stop the sweep without a new collector or paint timer.

Settings > Indicators adds “Show indicator names”, default on, stored as `show_metric_labels`. Turning it off removes only metric captions in both rotating and simultaneous layouts; retain rings, numbers, units, full accessible names and panel headings. Changes apply and resize immediately, preserve the selected metric, and settle any in-flight caption handoff. All four interface languages include this setting. It is independent of the five visibility toggles, so it cannot keep an otherwise disabled bar visible.
