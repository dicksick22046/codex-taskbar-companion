# 本次代码审核

范围：额度重置、鼠标与轮播、侧边聊天状态、阶段Token统计、源码入口和安装构建。不是完整安全审计或跨系统认证。

## 已修正

- 透明字形间隙会把鼠标交给Windows任务栏。整个交互矩形现在统一命中，组件外的空白继续穿透。
- 日单位按钮的悬停与刷新争用光标。两条路径已共用判断，悬停不会改变单位。
- 鼠标出入和关闭面板反复重置轮播计时。改为保留已等待进度。
- SQLite上下文管理器不会自动关闭连接。侧边聊天读取使用显式关闭，避免Windows文件句柄残留。
- Codex商店版日志可能位于MSIX LocalCache。读取同时定位普通安装与商店版目录，选择最新桌面进程，不依赖启动宿主的虚拟化视图。
- 源码移入包后，资源、自启动与构建入口同步指向正确位置；根app.py保留启动兼容。
- 删除过时的交接流水、首版审查和反复追加的样式记录，现行行为集中到架构说明与规范。

## 验证与边界

回归覆盖额度抖动与分类、幂等重置、侧聊关联及生命周期、普通分叉排除、历史阶段差分、日周边界、命中矩形和轮播暂停恢复。实际窗口另检验，真实重置不会被测试消费。

本次102项回归及根入口smoke-test通过。Windows 11安装版的指标/状态按钮逐点检查、任务区域连续命中、悬浮框打开时轮播、单位点击保存和历史用量显示均已验证。侧边聊天用本机真实日志关联回放以及模拟生命周期验证，没有额外发起聊天。覆盖安装保留设置、数据和系统启动登记，安装文件与本次构建一致。

侧边聊天依赖当前Codex日志格式；缺少元数据不能凭空恢复临时会话。Official为用户指定的排除法分类。历史Token限于本机已收录的持久化任务，不是跨设备账户账单。

macOS、Windows10、多屏独立状态条及完整无障碍支持尚未验证。安装器的签名信誉和真实重启登录不能由本机单元测试替代。
